# net/ — TCP port scanner
Checks which ports are open on a host using raw sockets. **Localhost / your own machines only.**
Run: `python port_scanner.py`.
Make it mine (TODO): threads for speed; map 22->SSH, 80->HTTP, 443->HTTPS; a timeout flag.
Inspired by github.com/itaynir1/port-scanner (my own code).
